#include "main.h"
#include <string.h>
/**
 * _strlen - Entry point
 * Description: 'This program returns the length of a string. '
 * @s: Pointer of char
 * Return: Integer
*/
int _strlen(char *s)
{
	return (strlen(s));
}
