#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _puts - Entry point
 * Description: 'This program prints a string,
 * followed by a new line, to stdout '
 * @str: Pointer of char
 * Return: Always void
*/
void _puts(char *str)
{
	puts(str);
}
