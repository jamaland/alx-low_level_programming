#include <stdlib.h>
#include "main.h"
/**
 * _abs - Entry point
 * @n: The  character to verify
 * Description: 'This program verify a character '
 * Return: 1 (Success) 0 (failure)
*/
int _abs(int n)
{
	return (abs(n));
}
