#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcpy - Entry point
 * Description: 'This program prints half of a string,
 * followed by a new line. '
 * @src: Pointer of a string
 * @dest: Pointer of a string
 * Return: Always void
*/
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
